spring.config.import=optional:file:.env[.properties]


spring.main.banner-mode=off
logging.level.org.springframework=ERROR

spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

spring.sql.init.mode=always
spring.sql.init.platform=postgres

spring.datasource.url=${POSTGRES_URL}
spring.datasource.username=${POSTGRES_USERNAME}
spring.datasource.password=${POSTGRES_PWD}
spring.jpa.show-sql=true

###
# HikariCP Properties
###

# spring.datasource.hikari.connection-timeout=60000
# spring.datasource.hikari.maximum-pool-size=200
# spring.datasource.hikari.minimum-idle=30
# spring.datasource.hikari.idle-timeout=3000
# spring.datasource.hikari.poolName=HikariCP
# spring.datasource.type=jdbc:com.zaxxer.hikari.HikariDataSource



# spring.datasource.hikari.jdbcUrl=${POSTGRES_URL}
# spring.datasource.hikari.username=${POSTGRES_USERNAME}
# spring.datasource.hikari.password=${POSTGRES_PWD}
# spring.datasource.hikari.driverClassName=com.mysql.jdbc.Driver